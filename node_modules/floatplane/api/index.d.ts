import { Core } from "../Core";
import { Client, Edge } from "../lib/types";
export declare type EdgesResponse = {
    edges: Edge[];
    client: Client;
};
export declare class Api extends Core {
    endpoints: {
        edges: string;
    };
    /**
     * Fetch edges from the floatplane api
     */
    edges: () => Promise<EdgesResponse>;
}
//# sourceMappingURL=index.d.ts.map